VoxelTest is a simple voxel renderer and game engine written in C++. 

It can compile to a Python module to support rendering of 3D arrays.

It will soon feature a Lua module to load and execute scripts. 
